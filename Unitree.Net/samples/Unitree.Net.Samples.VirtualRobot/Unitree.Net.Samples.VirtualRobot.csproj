<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <IsPackable>false</IsPackable>
    <RootNamespace>Unitree.Net.Samples.VirtualRobot</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <!-- The same engine the 3D simulator runs, so both answer the sport service identically. This
         sample is just the one without a window. -->
    <ProjectReference Include="..\..\src\Unitree.Net.Simulation\Unitree.Net.Simulation.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" />
  </ItemGroup>

</Project>
